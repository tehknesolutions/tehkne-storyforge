name: Storyforge Validation

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: storyforge-validation-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Environment diagnostics
        run: |
          node --version
          npm --version
          pwd
          ls -la
          test -f package.json
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Verify workspace install
        run: |
          npm ls --depth=0
          npm run typecheck:story-lab --if-present
      - name: Root static validation
        run: npm run validate
      - name: Story Lab executable probes
        run: npm run validate:story-lab-probes
      - name: Story Lab typecheck
        run: npm run typecheck:story-lab
      - name: Story Lab build
        run: npm run build:story-lab
